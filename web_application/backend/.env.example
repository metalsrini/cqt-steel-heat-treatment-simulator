# Environment Configuration for C-Q-T Steel Heat Treatment Simulator Backend

# Server Configuration
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=production

# CORS Configuration (comma-separated list of allowed frontend URLs)
ALLOWED_ORIGINS=https://your-frontend-app.onrender.com,https://your-custom-domain.com

# Logging Configuration
LOG_LEVEL=info

# Application Settings
RELOAD=false

# Optional: API Rate Limiting
MAX_REQUESTS_PER_MINUTE=100

# Optional: Computation Limits
MAX_SIMULATION_TIME_SECONDS=300
MAX_SPATIAL_POINTS=201